# Grumdrig game

# compile
-i grumdrig.asm node compiler.js -i ./bompton.js -o grumdrig.asm grumdrig.slog

# assemble
-i grumdrig.bin -i grumdrig.dasm node vm.js -a grumdrig.asm -d grumdrig.dasm -o grumdrig.bin

# execute
node vm.js -i ./bompton.js -l grumdrig.bin -r


# tests

# compile
-i test.asm node compiler.js -o test.asm test.slog

# assemble
-i test.bin -i test.dasm node vm.js -a test.asm -d test.dasm -o test.bin

# execute
node vm.js -l test.bin -r



# Map

# node bompton.js --generate-map > map.html


# Manuals

-i vm.html node_modules/marked/bin/marked.js -i pqvm.md -o vm.html
-i asm.html node_modules/marked/bin/marked.js -i assembler_manual.md -o asm.html
-i compiler.html node_modules/marked/bin/marked.js -i compiler.md -o compiler.html
